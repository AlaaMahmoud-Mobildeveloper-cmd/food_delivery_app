name: food_delivery_app
description: "A comprehensive Food Delivery App built with Flutter and Clean Architecture."

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  google_fonts: ^6.1.0
  equatable: ^2.0.5
  get_it: ^7.6.4
  dio: ^5.7.0
  shared_preferences: ^2.2.2
  # firebase_core: ^3.0.0
  # cloud_firestore: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
